# rtb-rl

Deep-RL low-latency RTB (real-time bidding) optimization engine — PoC.

> Full documentation is written at the end of the build. Quickstart: `pip install -e .` then
> `python -m rtb_rl.cli demo`.
