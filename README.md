# rtb-rl

Because RL doesn't need to be the unpopular guy at the classroom.
Deep-RL low-latency RTB (real-time bidding) optimization engine. PoC level.

> Quickstart: `pip install -e .` then
> `python -m rtb_rl.cli demo`.
